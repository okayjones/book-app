<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../layout/head') %>
</head>

<body>
    <header><a href="/">📚 Best Books! 📚</a></header>
    <main>
        <h1><%= saved.length %> Saved Books</h1>
        <% saved.forEach(book => { %>
        <section>
            <img src="<%= book.img%>">
            <h2><%= book.title %></h3>
                <h3><%= book.author %></h4>
                    <p><%= book.description %></p>
                    <a href="/books/<%=book.id%>"><button>Show Details</button></a>
        </section>
        <% }) %>
    </main>
    <footer></footer>
</body>

</html>